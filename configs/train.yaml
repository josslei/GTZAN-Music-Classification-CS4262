# Training Configuration
training:
  experiment_name: "cnn"
  max_epochs: 50
  lr: 1e-3
  weight_decay: 1e-2
  patience: 10
  log_dir: "outputs/logs"
  checkpoint_dir: "outputs/checkpoints"

model:
  num_classes: 10
