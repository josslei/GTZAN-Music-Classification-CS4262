# Experiment: LSTM 3-second segments
model_arch: "lstm"
segment_seconds: 3.0

# Overrides for training configuration
training:
  experiment_name: "lstm_3s"
  max_epochs: 100
  lr: 0.001
  weight_decay: 0.01
  patience: 10
