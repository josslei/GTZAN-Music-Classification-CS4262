# Experiment: CNN2D 10-second segments
model_arch: "cnn2d"
segment_seconds: 10.0

# Overrides for training configuration
training:
  experiment_name: "cnn2d_10s"
  max_epochs: 100
  lr: 0.001
  weight_decay: 0.01
  patience: 10
