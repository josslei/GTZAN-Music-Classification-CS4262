# Experiment: CNN2D 6-second segments
model: "cnn2d"
segment_seconds: 6.0

# Overrides for training configuration
training:
  experiment_name: "cnn2d_6s"
  max_epochs: 50
  lr: 0.001
  weight_decay: 0.01
  patience: 10
